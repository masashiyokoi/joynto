<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Joynto α version</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag('favicon.ico') %>
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <a class="navbar-brand" href="/">Joynto α.</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbar" aria-expanded="false" aria-label="ナビゲーションの切替">
          <span class="navbar-toggler-icon"></span><i class="fas fa-fire"></i>
        </button>

        <div class="collapse navbar-collapse" id="navbar">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= controller.controller_name == 'times_messages' ? 'active' : nil %>">
              <a class="nav-link" href="/times_messages">TIMES</a>
            </li>
            <% if user_signed_in? %>
            <li class="nav-item <%= controller.controller_name == 'matches' ? 'active' : nil %>">
              <a class="nav-link " href="/matches">MACHES <i class="fas fa-fire"></i></a>
            </li>
            <% end %>
            <li class="nav-item <%= controller.controller_name == 'users' ? 'active' : nil %>">
              <a class="nav-link " href="/users">USERS</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_user.name if user_signed_in?%></a>
              <div class="dropdown-menu" aria-labelledby="dropdown">
                <a class="dropdown-item" href="#">リンク1</a>
                <% if user_signed_in? %>
                  <%= link_to 'mypage', mypages_show_path, class: "dropdown-item" %>
                  <%= link_to 'logout', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                <% else %>
                  <%= link_to 'login', new_user_session_path, class: "dropdown-item" %>
                <% end %>
              </div>
            </li>
          </ul>
          <form class="form-inline my-2 my-md-0">
            <input class="form-control" type="search" placeholder="検索..." aria-label="検索...">
          </form>
        </div>
      </nav>
    </header>
    <div class="container">
      <% if notice %><p class="notice"><%= notice.inspect.to_s %></p><% end %>
      <% if alert %><p class="alert"><%= alert %></p><% end %>
      <div class="row">
        <div class="col-md-12">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
